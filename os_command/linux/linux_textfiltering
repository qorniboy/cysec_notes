
search file using grep

default
cat <file> | grep <search>

case insensitive
cat <file> | grep -i <search>

does not contain
cat <file> | grep -n <search>

sort result unique
cat <file> | grep <search> | sort -u

count by new line
cat <file> | wc -l

cutting table like string
cat <file> | cut -d <delimiter> -f <position starts from 1>